# Symmetry Tool

Tool in custom editor window allowing to mirror objects based on position of mirror transform. Working only along X axis of mirror and doesn't mirror rotation. Keeps track on mirrored objects and modify its position and destroy if needed.